# To-Do

- [ ] deserialize comments
    - off by default but can be enabled via flag
- [ ] begin_from_file
    - begin and immediately deserialize?
    - might be better to define defaults + comments then deserialize
    - variants: begin_from_file_path, begin_from_file_with_allocator, begin_from_file_path_with_allocator
- [ ] set comment character/string
    - by default: `#`
- [ ] set assignment operator character/string
    - by default: `=`
- [ ] rewrite how comments are stored so multiple comments can be appended

